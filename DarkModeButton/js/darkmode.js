function darkMode() {
    var element = document.body;
    //seleccioname todo el body con classlist
    //con el toggle si le digo q quiero el modo oscuro, me añade la clase dark, sino la saca.
    element.classList.toggle("dark");
 }